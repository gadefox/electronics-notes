#!/bin/rc

. /usr/local/libexec/rc/read
dir = `(dirname $0)

fn check {
  python3 $dir/check.py $dir/$1.fw > /dev/tty
  echo 'Press any key to continue...' > /dev/tty
  key = `read
  ~ $key () && exit
}

for (i in 0 1 4 5 6 7 10 21 1000 1001 1002)
  check $i
